// Re-export all constants for backward compatibility
export * from './linkStyles';
export * from './problems';
export * from './layout';

// Re-export types from types.ts
export type { CustomNode, CustomEdge, NodeData, Problem } from '../types';
